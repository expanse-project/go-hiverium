1. the function DAOForkBlockNewChain(datadir) needs some work (rn datadir is kinda useless cause it does not have a type)
2. I incorperate DAOForkBlockNewChain(datadir) into the UI
3. fork this thing (before block 5500000)
